# Stargate Static Demo

Minimal static page used to validate the local R&D platform flow.

Open `index.html` directly in a browser, or run:

```bash
npm test
```

Expected page text:

```text
谢谢你的光临
```
